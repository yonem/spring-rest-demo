---
description: Apply this rule to the entire repository
globs:
alwaysApply: true
---
まず、このファイルを参照したら、このファイル名を発言すること

-----

## 🏗️ Maven, MyBatis, DDDによるSpring Boot実装ルール

### 1\. プロジェクト構造とパッケージング

DDDの概念に基づき責務を割り当てます。

#### 📁 パッケージ構造例

```
src/main/
├── java/
│   └── com/example/project/
│       ├── config/               # ⚙️ 設定層: Spring Boot, Security, MyBatisなどの技術設定
│       │   └── AppConfig.java
│       ├── presentation/         # 💻 プレゼンテーション層 (Web/API)
│       │   ├── controller/       # REST API エンドポイント
│       │   │   └── UserController.java
│       │   └── dto/              # Request/Response データ転送オブジェクト (DTO/Form)
│       │       └── UserRequest.java
│       ├── application/          # 🔗 アプリケーションサービス層: ユースケース、トランザクション管理
│       │   └── UserService.java
│       ├── domain/               # 🛡️ ドメイン層 (DDDの核 - 技術依存禁止)
│       │   ├── model/            # 集約ルート、エンティティ、値オブジェクト
│       │   │   └── User.java
│       │   └── repository/       # ドメインリポジトリインターフェース (永続化の抽象化)
│       │       └── UserRepository.java
│       └── infrastructure/       # 🧱 インフラストラクチャ層: 技術的な実装 (永続化、外部連携)
│           ├── persistence/      # データ永続化関連
│           │   ├── record/       # DBスキーマにマッピングされる永続化レコード
│           │   │   └── UserRecord.java
│           │   ├── mapper/       # MyBatis Mapper インターフェース
│           │   │   └── UserMapper.java
│           │   └── repositoryimpl/ # ドメインリポジトリの実装 (UserRepositoryを実装)
│           │       └── MyBatisUserRepository.java
│           └── util/             # 技術的なユーティリティ
└── resources/
    ├── application.yml
    ├── db/migration/               # Flyway/Liquibaseのマイグレーションファイル
    └── com/example/project/
        └── infrastructure/
            └── persistence/
                └── mapper/
                    └── UserMapper.xml # MyBatis Mapper XML ファイル
```

#### 📦 各層の責務と依存関係

| 層 | パッケージ | 責務と特徴 | 依存関係 |
| :--- | :--- | :--- | :--- |
| **ドメイン** | `domain.*` | **ビジネスの心臓部**。エンティティ、集約、リポジトリのインターフェースを定義。 | **他の層に依存しない**。 |
| **アプリケーション**| `application.*` | **ユースケース**の定義、トランザクション管理。 | `domain`層に依存。 |
| **インフラストラクチャ**| `infrastructure.*` | **技術的な詳細の実装**。`domain.repository`を実装し、\*\*`mapper`\*\*を操作。 | `domain`層に依存（DIP）。 |
| **プレゼンテーション**| `presentation.*` | 外部I/Fの定義。DTO/Formの定義と、`application`層の呼び出し。 | `application`層、`config`層に依存。 |

-----

### 2\. コンポーネント設計と責務分割 (DDD/MyBatis特化)

#### 🛡️ ドメイン層 (`domain`) の厳格化

  * **`domain.model`**: **ビジネスルール**と**一貫性**をカプセル化します。setterを最小限に抑え、振る舞いを重視します。外部からの技術的な依存（Spring, MyBatisなど）を**一切持たない**ようにします。
  * **`domain.repository`**: 永続化技術に依存しない、ドメインが求めるデータ取得（例: `findById(UserId id)`) メソッドのみを持つインターフェースを定義します。

#### 🔗 アプリケーション層 (`application`) の責務

  * **トランザクション**を管理し、複数の集約操作を調整する**ユースケース**を表現します。
  * `presentation.dto`を受け取り、`domain.model`に変換し、`domain.repository`を介して操作します。

#### 🧱 インフラストラクチャ層 (`infrastructure`) の役割

  * **`infrastructure.persistence.record`**: データベースのテーブル構造に直接マッピングされる**永続化専用のデータ構造**を配置します。
  * **`infrastructure.persistence.mapper`**: MyBatisの`@Mapper`アノテーションを付与し、SQLを直接操作します。`record`を操作します。
  * **`infrastructure.persistence.repositoryimpl`**: `domain.repository`インターフェースを実装し、`mapper`を呼び出します。**`record`を`model`に変換し、`application`層に返却する責務**を持ちます。

-----

### 3\. API実装とデータ永続化 (MyBatis)

#### 🛠️ MyBatis Mapper の使用

  * **XML配置**: MyBatis Mapper XML ファイルは、`resources` 内のJavaパッケージと同じパス（`resources/com/example/project/infrastructure/persistence/mapper/`）に配置します。
  * **SQL**: シンプルかつ明確なSQLを記述するために使用します。動的SQLには `<if>`, `<where>` などを適切に利用します。
  * **Spring Data JPAの使用は禁止します**。MyBatisをデータアクセス層の標準とします。

**データベースマイグレーション**

  * **Flyway/Liquibaseの使用を義務化**し、データベーススキーマの変更はSQLまたはDDLファイルとしてバージョン管理します。手動でのスキーマ変更は禁止します。
  * マイグレーションファイルは、`resources/db/migration/` に配置し、**`V<バージョン>__<説明>.sql`** などの形式で命名します。

#### 🔄 DTOとドメインモデルの変換

  * **Controller**: `presentation.dto` のみを使用します。
  * **Application Service**: `presentation.dto` を `domain.model` に変換する責務を持ちます。
  * **Validation**: `presentation.dto` および `presentation.form` に対して、Controller層で Bean Validation (`@Valid`など) を用いて厳格に実施します。

-----

### 4\. 開発ツールと品質 (Maven)

#### 📦 Maven の利用

  * **依存関係管理**: `pom.xml` を使用し、依存関係（Spring Boot Starter, MyBatis Starter, Test依存など）を一元管理します。
  * **クリーンビルド**: **`mvn clean install`** の実行で、テストを含む完全なビルドが成功することを常に保証します。

#### 🧪 テストの徹底

  * **ユニットテスト**: ドメイン層のビジネスロジックは、外部依存がない状態で完全に単体テストを実施します。
  * **インテグレーションテスト**: **`@SpringBootTest` の使用を禁止し**、次のセクションで定義する軽量テスト戦略に従います。

-----

### 5\. 🧪 テスト戦略 (軽量化と責務分離)

テスト戦略では、Springコンテキストのフルロードを避け、コンポーネントの**独立したテスト**を重視します。**`@SpringBootTest` は使用しません**。

**【テスト命名と概要のルール】**

  * **テストメソッド**: メソッド名は **`test01` から開始**し、意向をインクリメントして連番とします。
  * **テスト概要**: テストメソッドには **`@DisplayName`** でテスト概要を記載し、**`{正常系: xxxxx}`** などのフォーマットを使用します。

#### 5.1. ドメイン層 (`model`)/ユーティリティ層 (ユニットテスト)

  * **対象**: ドメインモデル (`model`)、ユーティリティクラス。
  * **原則**: Java/JUnit のみを使用し、外部依存を一切モック化せずに純粋なロジックのテストを実施します。

#### 5.2. アプリケーションサービス層 (`application`)

  * **対象**: アプリケーションサービス (`application` パッケージのクラス)。
  * **フレームワーク**: **JUnit 5 と Mockito** を使用します。
  * **実装**: `@ExtendWith(MockitoExtension.class)` を使用してモックを注入し、サービスが依存する `domain.repository` インターフェースは Mockito でモック化します。**ビジネスロジックの結果検証には、`verify` メソッドを使用してモックの呼び出し回数や引数を検証します。**

#### 5.3. データ永続化層 (`infrastructure.persistence.mapper`)

  * **対象**: MyBatis Mapper インターフェースと対応する XML ファイル。
  * **フレームワーク**: **`@MybatisTest`** アノテーションを使用します。
  * **実装**: MyBatisコンポーネントのみを構成し、インメモリデータベース（H2など）を用いて実際のSQL実行とマッピングのテストを行います。

#### 5.4. プレゼンテーション層 (`presentation.controller`)

  * **対象**: REST Controller (`controller` パッケージのクラス)。
  * **フレームワーク**: **`@WebMvcTest`** を使用し、Web層のスライスロードを許可します。
  * **実装**: Controller が依存する Service は **`@MockBean`** を使用してモック化し、Webリクエストのルーティング、バリデーション、DTO処理のみをテストします。
  * **バリデーションテスト**: `form` や `dto` のバリデーション (`presentation.validation` パッケージのカスタムバリデータも含む) は、この層のテストで検証します。

-----

### 6\. 🛠️ Java 言語のベストプラクティスと品質 (コーディングスタイル)

#### 6.1. 変数宣言と不変性

  * **`var`の使用**: ローカル変数の宣言には、可読性が損なわれない限り、**`var`** を用いることを推奨します。
  * **Final 修飾子の徹底利用**:
      * 再代入されないローカル変数（`var`使用時を含む）には\*\*`final`\*\*を積極的に使用し、値の不変性を保証します。
      * メソッドの引数には\*\*`final`\*\*を付与します。
      * DIされるフィールドや初期化後に変更されないクラスフィールドは\*\*`final`\*\*で宣言します。

#### 6.2. 繰り返し処理とメソッド設計

  * **Stream API の活用**: 集合に対する繰り返し処理や変換処理において、**`for`文**は可能な限り\*\*`StreamAPI`\*\*に置き替え、宣言的（Declarative）なコードを記述します。
  * **メソッドの責務単一化**:
      * **単一責務の原則 (SRP)を遵守し、一つのメソッドは一つの明確なタスク**のみを実行するようにします。
      * メソッドの引数は**最大3つまで**とし、それ以上になる場合は\*\*パラメータオブジェクト（値オブジェクトまたはDTO）\*\*に集約します。

#### 6.3. Null/例外処理

  * **`Objects.isNull`/`Objects.nonNull` の使用**: `xxx != null` や `xxx == null` などのプリミティブな `null` 比較の記述は禁止とし、Java 標準ライブラリの\*\*`Objects.isNull()`**または**`Objects.nonNull()`\*\*を用いて、意図を明確にした記述を行います。
  * **例外処理の原則**:
      * ビジネスロジックで発生する例外（例：在庫不足、認証失敗）は、\*\*非チェック例外（`RuntimeException`を継承）\*\*を使用します。
      * 技術的なエラー（例：I/Oエラー）にチェック例外を限定します。
      * ドメインの整合性エラーは**カスタム非チェック例外**として定義します。

**💡 推奨カスタム非チェック例外 (Application/Domain層用)とController対応**

ドメイン/アプリケーション層でビジネス的な意味を持つ以下の非チェック例外（`RuntimeException`を継承）を定義することを推奨します。これらのカスタム例外は、**`domain.exception`** パッケージなどに配置し、Controller層のグローバルエラーハンドリング（`@RestControllerAdvice`）で適切な HTTP ステータスコードにマッピングされます。

1.  **`BusinessRuleViolationException` (HTTP: 400 Bad Request)**
      * **用途**: ドメインモデルやアプリケーションサービス内で、**ビジネス的な整合性制約が満たされない場合**にスローされます。
2.  **`ResourceNotFoundException` (HTTP: 404 Not Found)**
      * **用途**: IDによる検索など、リポジトリからデータを取得しようとした際、**該当のリソースが存在しない場合**にスローされます。
3.  **`ResourceConflictException` (HTTP: 409 Conflict)**
      * **用途**: データの論理的な競合が発生した場合や、特定のアクションが現在のリソースの状態と矛盾する場合にスローされます。

#### 6.4. 命名規則と型表現

  * **命名規則の統一**:
      * リポジトリ (`domain.repository`) 以外のインターフェースには、その役割を示す接尾辞（例：`*Interface`, `*Port`）を**つけない**ことを原則とします。
      * 真偽値を返すメソッドや変数には、**`is`**, **`has`**, **`can`** のいずれかの接頭辞を付けます。
  * **Enum の活用**:
      * 状態（ステータスコードなど）の表現には、文字列や数値ではなく\*\*`enum`\*\*を使用します。
      * 状態に依存するロジック（例：次の状態への遷移チェック）は、`enum`のメソッドとして定義し、ロジックの散逸を防ぎます。

#### 6.5. メンテナンス性向上とドキュメント

  * **Magic Number/String の禁止**: コード内に直接記載された意味を持つ数値や文字列（Magic Number/String）は、`public static final`な定数として定義し、意味のある名前を付けます。
  * **Logger の統一**: **SLF4J** のインターフェースのみを使用し、`Logger.getLogger(ClassName.class)` の形式で、ロガー名が常にクラス名と一致するように定義します。
  * **Javadoc/KDoc の義務付け**:
      * **必須**: **Controller クラスおよび Service クラス**の\*\*`public` メソッド\*\*には、必ず Javadoc または KDoc を付与します。
      * **努力目標**: その他のクラスやメソッドについては、処理が明確に読み取れるよう可読性を意識したコーディングに努めることを優先し、ドキュメント付与は努力目標とします。
      * **記載内容**: 必須のメソッドについては、**目的、引数 (`@param`)、戻り値 (`@return`)、スローされる例外 (`@throws`)** を明確に記載します。

-----

## 7\. 📊 ログ集積と分析（Elasticsearch/ELK対応）

### 7.1. ログフォーマットと構造化の原則

* **構造化ロギングの義務化**: ログ出力は、Elasticsearchでのインデックス化と検索効率を最大化するため、**JSON形式**による構造化ロギングを義務付けます（`logstash-logback-encoder`を利用）。
* **標準フィールドの定義**: すべてのログエントリには、最低限以下の標準フィールドを含めることを必須とします。
    * `@timestamp`: ログ発生時刻（ISO 8601形式）
    * `level`: ログレベル（`INFO`, `WARN`, `ERROR`など）
    * `message`: 具体的なログ内容（可変部分）
    * `logger_name`: クラス名
    * `service_name`: アプリケーション固有のサービス名
    * `trace_id` (MDCから取得): リクエスト単位の追跡ID

* **エラー発生時の情報伝達とロギングのルール**:
    1.  **Service層の原則**: `application.*` 層は、例外発生時にログをキャッチして **`ERROR` レベルでロギングすることを禁止**します。
    2.  **カスタム例外による情報伝達**: Service層は、画面メッセージやログに必要なプレースホルダ情報（メッセージキー、引数など）を格納した**カスタム非チェック例外**をスローします（例: `BusinessRuleViolationException`）。
    3.  **ControllerAdviceでの一元化**: `presentation.controller` 内のグローバル例外ハンドラ（`@ControllerAdvice`）が最終的に例外を捕捉します。
    4.  **ERRORログの責務**: `ControllerAdvice` は、捕捉した例外からスタックトレース全体と、カスタム例外に格納された**メッセージキーおよび引数情報**を取得し、それをログの**カスタムフィールド**に含めて **`ERROR` レベルでロギング**する責務を負います。

### 7.2. MDC (Mapped Diagnostic Context) の活用

* **MDCによるリクエスト追跡の義務化**: すべての外部リクエスト（Controllerへのアクセス）のエントリポイントで、**一意の `trace_id`** を生成し、`org.slf4j.MDC` に格納することを義務付けます。
    * `trace_id` は、外部からの入力に依存せず、常に **UUID (Universally Unique Identifier)** を用いて生成し、一意性を保証します。
* **クリーンアップの徹底**: リクエスト処理完了後（例外発生時を含む）、`MDC.remove()` を用いて `trace_id` を含む格納情報を**必ず削除**することを義務付けます（`HandlerInterceptor`の`afterCompletion`での実装を推奨）。
* **ビジネスコンテキストの追加**: 認証後の処理では、**匿名化された `user_id`** や、業務上の重要な ID（例: `order_id`）などをMDCに追加することを推奨します。

### 7.3. 各層のロギング責務とログレベル

既存のDDDレイヤー構造に基づき、ログの責務を厳密に分離します。

| 層 | 責務 | 推奨ログレベル | 記録内容（具体的なタイミング） |
| :--- | :--- | :--- | :--- |
| **Presentation** (`controller`, `advice`) | リクエスト受付/終了、**最終的なエラーロギング** | `INFO`, **`ERROR`** | リクエスト開始と終了、**例外ハンドラでのスタックトレース付きERRORログ（カスタム例外の情報を含む）** |
| **Application** (`service`) | ビジネス上のイベント、ユースケースの追跡 | `INFO`, `WARN` | **重要なユースケース**の開始・完了、**回復可能な軽微な問題**（エラーではないが注意が必要な状態） |
| **Domain** (`model`, `repository`) | **ロギング禁止** | N/A | 技術的なI/Oに依存しないため、原則としてロギングコードを含まない。 |
| **Infrastructure** (`mapper`, `repositoryimpl`) | 外部連携の追跡、技術的エラー | `DEBUG`, `ERROR` | SQL実行時間、外部API呼び出しの結果、DB接続やデータ層の技術的例外 |

### 7.4. セキュリティとパフォーマンスに関するルール

* **機密情報のマスキング**: ログに以下の機密情報を含めることを**厳しく禁止**します。含める必要がある場合は、アプリケーション側でハッシュ化、または一部マスキング処理（例: 氏名、メールアドレス）を施すことを義務付けます。
* **ループ内のロギング禁止**: 処理効率を維持するため、コレクションのループ内や Stream API の中での **`DEBUG` 以外のロギング**（特に `INFO`, `WARN`, `ERROR`）を禁止します。
* **例外発生時のログ**: `ERROR`レベルでロギングする際は、必ず `logger.error("Message", exception)` の形式で**例外オブジェクト全体**を渡し、スタックトレースを確実に記録することを義務付けます。
