<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.ne.yonem.restful.mapper.PasswordPolicyMapper">
    <select id="findById">
        SELECT *
        FROM password_policy
        WHERE id = #{id}
    </select>
</mapper>